// import { getFile } from "@/lib/uploadsVault";

// const file = getFile(form.logoFileId);
// if (file) {
//   const fd = new FormData();
//   fd.append("logo", file);
//   // fetch("/api/upload", { method: "POST", body: fd });
// }
